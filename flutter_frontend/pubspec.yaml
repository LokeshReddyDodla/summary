name: report_summarization_frontend
description: Flutter client for the Report Summarization Bot FastAPI backend.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  file_picker: ^8.0.2
  url_launcher: ^6.2.6

dev_dependencies:
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
